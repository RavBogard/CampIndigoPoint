---
phase: 02
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/site/SiteLayout.jsx
  - src/components/site/SiteHeader.jsx
  - src/components/site/MobileNavDrawer.jsx
  - src/components/site/AudiencePathGrid.jsx
  - src/components/site/AnnouncementBar.jsx
  - src/routes/HomeRoute.jsx
  - src/index.css
  - src/content/site.js
  - src/content/home.js
autonomous: true
user_setup: []
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
must_haves:
    truths:
      - "Families, donors, and staff are visibly represented within the first viewport of the homepage."
      - "Desktop and mobile navigation expose the primary site destinations persistently through the shared shell."
      - "Register and Donate remain consistent shell-level actions rather than page-specific one-offs."
    artifacts:
      - path: "src/components/site/SiteHeader.jsx"
        contains: "Register|Donate|Apply"
        min_lines: 30
      - path: "src/components/site/MobileNavDrawer.jsx"
        contains: "About|FAQ|Contact"
        min_lines: 30
      - path: "src/routes/HomeRoute.jsx"
        contains: "audiencePaths"
        min_lines: 30
      - path: "src/components/site/AnnouncementBar.jsx"
        contains: "announcement"
        min_lines: 20
    key_links:
      - from: "src/components/site/SiteHeader.jsx"
        to: "src/content/actions.js"
        via: "persistent CTA cluster"
        pattern: "actionLinks|defaultCtas|register|donate|apply"
      - from: "src/routes/HomeRoute.jsx"
        to: "src/content/home.js"
        via: "audience gateway"
        pattern: "audiencePaths|hero|quickFacts"
      - from: "src/components/site/AnnouncementBar.jsx"
        to: "src/content/site.js"
        via: "seasonal shell banner"
        pattern: "announcement|enabled|action"
---

# Phase 2 Plan 02: Build Navigation, Homepage Pathing, and Persistent Actions

## Objective
- **What:** Implement the visible shell behaviors that make the site feel like Camp Indigo Point immediately: navigation, audience pathing, announcement handling, and persistent action links.
- **Why:** These are the user-facing behaviors that satisfy the core routing/navigation requirements and establish how visitors self-sort into family, donor, and staff journeys.
- **Output:** Shared header and mobile drawer components, homepage gateway composition, announcement bar wiring, and persistent shell CTAs backed by centralized content.

## Context
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-site-shell-and-audience-routing/02-CONTEXT.md
@.planning/phases/02-site-shell-and-audience-routing/02-RESEARCH.md
@.planning/phases/02-site-shell-and-audience-routing/02-VALIDATION.md

## Tasks

<task type="auto">
  <name>Implement shared desktop and mobile navigation</name>
  <files>src/components/site/SiteLayout.jsx, src/components/site/SiteHeader.jsx, src/components/site/MobileNavDrawer.jsx, src/index.css</files>
  <action>Create a shell header that includes direct top-level Families, Donors, and Staff pathways alongside core trust pages, plus a simple mobile drawer that exposes the same information architecture cleanly on small screens.</action>
  <verify>Run `npm run lint` and `npm run build`, then manually inspect desktop and mobile breakpoints to confirm all Phase 2 required destinations can be reached from persistent navigation.</verify>
  <done>The shell has a recognizable, route-aware desktop/mobile navigation model instead of a portfolio-style static layout.</done>
</task>

<task type="auto">
  <name>Make homepage audience routing and shell CTAs explicit</name>
  <files>src/routes/HomeRoute.jsx, src/components/site/AudiencePathGrid.jsx, src/components/site/SiteHeader.jsx, src/index.css, src/content/home.js</files>
  <action>Compose the homepage as a gateway with hero copy, audience path cards, and clear shell-level Register, Donate, and Apply actions so the first viewport quickly answers “where do I go next?” for each audience.</action>
  <verify>Run `npm run lint`, then manually review the homepage at desktop and mobile sizes to confirm Families, Donors, and Staff are all visible above or at the top of the initial screen experience.</verify>
  <done>The homepage actively routes visitors toward the right journey instead of acting like a generic one-page landing page.</done>
</task>

<task type="auto">
  <name>Wire the announcement bar and canonical CTA sources into the shell</name>
  <files>src/components/site/AnnouncementBar.jsx, src/components/site/SiteLayout.jsx, src/components/site/SiteHeader.jsx, src/content/site.js</files>
  <action>Render the seasonal announcement and persistent actions directly from the Phase 1 site settings and action registry so the shell stays aligned with centralized content operations.</action>
  <verify>Run `npm run build` and manually inspect that header CTAs and announcement behavior are reading from `src/content/site.js` and `src/content/actions.js` rather than hard-coded route component strings.</verify>
  <done>Shell-level action visibility and announcement behavior are centralized and maintainable.</done>
</task>

## Verification
- `npm run lint`
- `npm run build`
- Manual desktop/mobile review that navigation remains persistent and the homepage exposes Families, Donors, and Staff immediately.

## Success Criteria
- Desktop and mobile navigation expose the required destinations consistently.
- The homepage makes family, donor, and staff pathways obvious near the top of the experience.
- Register and Donate remain visible and consistent as shell-level actions.
- The seasonal announcement pattern is rendered from the centralized content layer.
